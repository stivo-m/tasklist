enum AppContext {
  development,
  testing,
  production,
}

enum TaskStatus {
  pending,
  completed,
  unknown,
}

enum AuthenticationStatus {
  unknown,
  authenticated,
}

enum TasListType {
  systemDefault,
  user,
}
